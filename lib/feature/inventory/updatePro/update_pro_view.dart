import 'package:flutter/widgets.dart';

class UpdateProView extends StatelessWidget {
  const UpdateProView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}